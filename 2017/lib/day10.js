// @flow
const { range } = require('./utils');

const knotHash = (lengths: number[]): number[] => {
  let list = range(0, 255);
  let currentPos = 0;
  let skipSize = 0;

  lengths.forEach(len => {

  });

  return list;
}

module.exports = { knotHash };